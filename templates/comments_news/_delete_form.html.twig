<form method="post" action="{{ path('comments_news_delete', {'id': comments_news.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comments_news.id) }}">
    <button class="btn">Delete</button>
</form>
