{% extends 'base.html.twig' %}

{% block title %}CategoryForum index{% endblock %}

{% block body %}
    <h1>Le Forum Des Managers</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>NameCategory</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for category_forum in category_forums %}
            <tr>
                <td>{{ category_forum.id }}</td>
                <td><a href="{{ path('category_forum_show', {'id': category_forum.id}) }}">{{ category_forum.nameCategory }}</a></td>
                <td>
                    <a href="{{ path('category_forum_show', {'id': category_forum.id}) }}">show</a>
                    <a href="{{ path('category_forum_edit', {'id': category_forum.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('post_forum_new') }}">Créer un nouveau topic</a>
{% endblock %}
